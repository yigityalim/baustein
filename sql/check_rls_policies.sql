-- Check current RLS policies on study_groups table
SELECT 
  schemaname,
  tablename,
  policyname,
  permissive,
  roles,
  cmd,
  qual,
  with_check
FROM pg_policies 
WHERE tablename = 'study_groups'
ORDER BY policyname;
